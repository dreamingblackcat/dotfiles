# ~/.ruby_managers.sh — Optional Ruby version managers
# Source this file if you need RVM, rbenv, or pyenv
# Uncomment the manager you want to use (typically only one is needed)
# mise is the primary version manager in this dotfiles setup

# ── RVM (Ruby Version Manager) ────────────────────────────────────────────────
# Uncomment to use RVM instead of mise
# export PATH="$PATH:$HOME/.rvm/bin"
# [ -f "$HOME/.rvm/scripts/rvm" ] && source "$HOME/.rvm/scripts/rvm"

# ── rbenv (Ruby Environment) ──────────────────────────────────────────────────
# Uncomment to use rbenv instead of mise
# export PATH="$PATH:$HOME/.rbenv/bin"
# eval "$(rbenv init -)"

# ── pyenv (Python Environment) ────────────────────────────────────────────────
# Uncomment to use pyenv instead of mise
# export PATH="$PATH:$HOME/.pyenv/bin"
# eval "$(pyenv init -)"

# ── nvm (Node Version Manager) ────────────────────────────────────────────────
# Uncomment to use nvm instead of mise
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
